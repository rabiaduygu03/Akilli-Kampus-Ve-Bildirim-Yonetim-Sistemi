using System;

namespace SmartCampus.Infrastructure
{
    public class DatabaseConnection
    {
        public void Connect()
        {
            // İleride gerçek bir SQL veritabanı eklendiğinde 
            // bağlantı ayarları bu katmanda yapılacaktır.
            Console.WriteLine("Veritabanı bağlantısı hazır.");
        }
    }
}